spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_db
    username: thabiso
    password: crypto007#
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  sql:
    init:
      mode: always
